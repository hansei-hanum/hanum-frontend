import styled from '@emotion/native';

import { colors } from 'src/styles';

export const TeacherVerifyWrapper = styled.SafeAreaView`
  flex: 1;
  background-color: ${colors.background};
`;

export const TeacherVerifyHeaderWrapper = styled.View`
  padding: 16px;
`;
