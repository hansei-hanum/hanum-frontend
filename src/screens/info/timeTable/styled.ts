import styled from '@emotion/native';

export const TimeTableNoDataContainer = styled.View`
  flex: 1;
  justify-content: center;
  align-items: center;
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  row-gap: 10px;
`;

export const TimeTableNoDataTextContainer = styled.View`
  align-items: center;
  justify-content: center;
  row-gap: 4px;
`;
