import styled from "@emotion/native";
import { colors } from "src/styles";

export const StatusRaffleWrapper = styled.View`
    width: 90px;
    height: 90px;
    align-items: center;
    justify-content: center;
    border-radius: 90px;
    background-color: ${colors.primary};
`;