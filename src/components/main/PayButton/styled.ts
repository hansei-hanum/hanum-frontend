import styled from '@emotion/native';

import { colors } from 'src/styles';

export const PayButtonElement = styled.TouchableOpacity`
  width: 49%;
  border-radius: 14px;
  align-items: center;
  justify-content: center;
  background-color: ${colors.lightGray};
  padding: 10px 0;
`;
