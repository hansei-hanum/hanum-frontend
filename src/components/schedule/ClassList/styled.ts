import styled from '@emotion/native';

import { RPH, RPW } from 'src/utils';

export const ClassListWrapper = styled.View`
  width: ${`${RPW(17)}px`};
  height: ${`${RPH(9)}px`};
  align-items: center;
  justify-content: center;
  text-align: center;
  padding-right: ${`${RPW(1.6)}px`};
`;
