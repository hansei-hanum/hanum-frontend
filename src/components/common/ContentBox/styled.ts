import styled from '@emotion/native';

import { colors } from 'src/styles';

export const ContentBoxContainer = styled.SafeAreaView`
  width: 100%;
  border-radius: 16px;
  background-color: ${colors.white};
`;
