import styled from '@emotion/native';

import { colors } from 'src/styles';

export const IconWrapper = styled.SafeAreaView`
  padding: 4px;
  border-radius: 10px;
  background-color: ${colors.lightGray};
`;
